import { DirectBackgroundDirective } from './direct-background.directive';

describe('DirectBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
