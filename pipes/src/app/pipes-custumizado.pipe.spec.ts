import { PipesCustumizadoPipe } from './pipes-custumizado.pipe';

describe('PipesCustumizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesCustumizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
